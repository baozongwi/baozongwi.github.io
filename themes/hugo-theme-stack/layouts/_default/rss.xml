{{- /* Restrict RSS items to posts only; for home, only homepage paginator */ -}}
{{- $baseTitle := .Site.Title -}}
{{- $items := slice -}}
{{- if .IsHome -}}
  {{- $pages := where .Site.RegularPages "Type" "in" .Site.Params.mainSections -}}
  {{- $notHidden := where $pages "Params.hidden" "!=" true -}}
  {{- $p := .Paginate $notHidden -}}
  {{- $items = $p.Pages -}}
{{- else -}}
  {{- $pages := where .Pages "Type" "in" .Site.Params.mainSections -}}
  {{- $items = where $pages "Params.hidden" "!=" true -}}
{{- end -}}
{{- "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" | safeHTML -}}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>{{ $baseTitle }}</title>
    <link>{{ .Permalink }}</link>
    <description>{{ default $baseTitle .Site.Params.description }}</description>
    <lastBuildDate>{{ now.Format "Mon, 02 Jan 2006 15:04:05 -0700" }}</lastBuildDate>
    <atom:link href="{{ .Permalink }}" rel="self" type="application/rss+xml" />

    {{- range $items -}}
    {{- $desc := .Params.description -}}
    {{- if not $desc -}}
      {{- $desc = (.Summary | plainify) -}}
    {{- end -}}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <guid>{{ .Permalink }}</guid>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" }}</pubDate>
      <description>{{ $desc | htmlEscape }}</description>
      <content:encoded>{{ .Plain | truncate 150 | htmlEscape }}</content:encoded>
    </item>
    {{- end -}}
  </channel>
</rss>
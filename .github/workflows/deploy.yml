name: Deploy Hugo Site

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: "0.163.1"
          extended: true

      - uses: actions/cache@v4
        with:
          path: resources/_gen
          key: hugo-images-${{ hashFiles('hugo.yaml', 'layouts/**', 'themes/flavor/layouts/**') }}
          restore-keys: |
            hugo-images-

      - uses: actions/configure-pages@v5

      - run: hugo --minify --buildFuture --cleanDestinationDir

      - name: Build Pagefind search index
        run: npx -y pagefind@1.5.0 --site public

      - uses: actions/upload-pages-artifact@v4
        with:
          path: ./public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
